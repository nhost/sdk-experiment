{
  "name": "@nhost/docs",
  "version": "2.31.0",
  "private": true,
  "scripts": {
    "dev": "mintlify dev",
    "start": "mintlify dev",
    "build": "echo \"No build required for docs\" && exit 0",
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "pnpm audit && echo \"No lint specified\" && exit 0",
    "lint:fix": "echo \"No lint:fix specified\" && exit 0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "echo \"No typecheck required for docs\" && exit 0"
  },
  "devDependencies": {
    "mintlify": "^4.0.476"
  }
}
