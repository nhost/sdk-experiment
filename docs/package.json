{
  "name": "@nhost/docs",
  "version": "2.31.0",
  "private": true,
  "scripts": {
    "dev": "mintlify dev",
    "start": "mintlify dev",
    "build": "echo \"No build required for docs\" && exit 0",
    "test": "echo \"No test required for docs\" && exit 0",
    "lint": "echo \"No lint specified\" && exit 0",
    "lint:fix": "echo \"No lint:fix specified\" && exit 0",
    "format": "echo \"No format specified\" && exit 0",
    "typecheck": "echo \"No typecheck required for docs\" && exit 0"
  },
  "devDependencies": {
    "mintlify": "^4.0.476"
  }
}
